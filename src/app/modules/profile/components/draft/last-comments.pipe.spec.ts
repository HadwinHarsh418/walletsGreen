import { LastCommentsPipe } from './last-comments.pipe';

describe('LastCommentsPipe', () => {
  it('create an instance', () => {
    const pipe = new LastCommentsPipe();
    expect(pipe).toBeTruthy();
  });
});
